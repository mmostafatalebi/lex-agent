# The backend API URL. Point at your deployed Lambda + API Gateway.
NEXT_PUBLIC_API_URL=http://localhost:9000

# Whether the backend requires an x-api-key header.
NEXT_PUBLIC_REQUIRE_API_KEY=false

# Enable the in-memory mock backend for local development without the real API.
NEXT_PUBLIC_USE_MOCK=true
